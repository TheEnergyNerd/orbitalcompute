{
  "buildCommand": "[ -d frontend ] && cd frontend; npm ci && npm run build",
  "outputDirectory": ".next",
  "installCommand": "[ -d frontend ] && cd frontend; npm ci",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://your-railway-app.railway.app/:path*"
    }
  ]
}

